describe("lido", () => {
  describe("deposit", () => {
    beforeAll(async () => {})

    it("allows submitting ETH", async () => {})

    it("allows wrapping and unwrapping stETH", async () => {})
  })
})
