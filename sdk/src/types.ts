export enum Action {
  swap = "swap",
  deposit = "deposit",
  withdraw = "withdraw",
  stake = "stake",
  unstake = "unstake",
  claim = "claim",
  lend = "lend",
  borrow = "borrow",
  repay = "repay",
}

export enum Protocol {
  curve = "curve",
}
