# API

The API wraps the [SDK](/learn/sdk) in a simple REST interface so that it can be consumed by any programming language.
It is completely stateless and easily can be self-hosted.
We provide a free to use instance of the API at TODO.
