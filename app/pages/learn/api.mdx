# API

The API wraps the [SDK](/learn/sdk) in a simple REST interface so that it can be consumed by any programming language.
It is completely stateless and can easily be self-hosted.
We host a public instance of the API that is free to use without any limits.
