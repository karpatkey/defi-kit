# SDK

The SDK provides the DeFi permission presets as a TypeScript library that can be installed from npm.
It is based on the [Roles SDK](https://github.com/gnosis/zodiac-modifier-roles/tree/main/packages/sdk).
