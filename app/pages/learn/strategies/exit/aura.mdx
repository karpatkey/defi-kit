# Aura Exit Strategies

## `bpt`

Withdraw the Balancer pool token (BPT) balance staked in the Aura pool.

## `underlying_single`

Withdraw the Balancer pool token (BPT) balance staked in the Aura pool and redeem it, exiting the Balancer pool into the designated single token.
